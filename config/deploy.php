<?php

return [

    /*
    |--------------------------------------------------------------------------
    | مفتاح النشر عبر الويب
    |--------------------------------------------------------------------------
    |
    | مسار /deploy ميّتٌ ما لم يُضبط هذا المفتاح، فلا يُفتح باب النشر بالخطأ.
    | يُمرَّر في ترويسة X-Deploy-Token أو في ?token= ويُقارن مقارنةً ثابتة الزمن.
    | ولّده مرةً واحدة بمقدارٍ كافٍ من العشوائية، مثلاً:
    |
    |   php artisan tinker --execute="echo Str::random(48);"
    |
    */

    'token' => env('DEPLOY_TOKEN'),

    /*
    |--------------------------------------------------------------------------
    | مفتاح الإيقاف
    |--------------------------------------------------------------------------
    |
    | لإغلاق المسار دون حذف المفتاح — مفيدٌ أثناء صيانةٍ مطوّلة.
    |
    */

    'enabled' => (bool) env('DEPLOY_ENABLED', true),

    /*
    |--------------------------------------------------------------------------
    | قفل التزامن
    |--------------------------------------------------------------------------
    |
    | ثوانٍ يُمسك فيها القفل، فلا يتداخل نشرانِ على المستودع نفسه. اجعله أطول
    | من أطول نشرٍ متوقَّع، فالقفل يُحرَّر في كل الأحوال عند انتهاء الطلب.
    |
    */

    'lock_seconds' => (int) env('DEPLOY_LOCK_SECONDS', 1800),

    /*
    |--------------------------------------------------------------------------
    | الخادم البعيد
    |--------------------------------------------------------------------------
    |
    | ما يحتاجه أمر app:deploy-remote ليُطلق النشر من جهاز المطوّر: عنوان
    | الموقع ومفتاحه. المفتاح هنا هو DEPLOY_TOKEN المضبوط هناك، لا مفتاح هذه
    | النسخة، فلذلك اسمٌ منفصل يمنع الخلط بين البيئتين.
    |
    */

    'remote' => [
        'url' => env('DEPLOY_REMOTE_URL'),
        'token' => env('DEPLOY_REMOTE_TOKEN'),
    ],

];
